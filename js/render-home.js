function card(p){return `<div class='card-product'><img src='${p.images[0]}'><h3>${p.name}</h3><a class='btn' href='product.html?id=${p.id}'>Ver detalhes</a></div>`;} document.addEventListener('DOMContentLoaded',()=>{document.getElementById('showcase-grid').innerHTML=products.filter(p=>p.featured).map(card).join('');});